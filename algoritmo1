Crie um Array de inteiros com 3 elementos e imprima a soma dos valores armazenados no Array.


package main

import "fmt"

func main() {
	array := [3]int{1, 2, 3}
	sum := 0
	for i := 0; i < len(array); i++ {
		sum += array[i]
	}
	fmt.Println("A soma dos elementos é:", sum)
}
