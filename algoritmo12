package main

import "fmt"

func main() {
	array := [5]int{1, 3, 5, 6, 9}

	slice := []int{}
	for _, valor := range array {
		if valor%3 == 0 {
			slice = append(slice, valor)
		}
	}

	fmt.Println(slice)
}
