package main

import "fmt"

func main() {
	arr := [10]float64{1.5, 6.8, 3.2, 9.1, 2.3, 7.0, 5.5, 4.0, 8.7, 6.1}
	var slice []float64

	for _, num := range arr {
		if num > 5 {
			slice = append(slice, num)
		}
	}

	fmt.Println("Resultado:", slice)
}
